<resources>
    <!-- Base application theme -->
    <style name="Base.Theme.SunwaySportsLink" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize primary and status bar colors -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:colorBackground">@color/white</item> <!-- Black background -->
        <item name="android:textColorPrimary">@color/black</item> <!-- White text -->
        <item name="android:windowLightStatusBar">true
        </item> <!-- Ensures black icons in light status bar -->
    </style>
</resources>
